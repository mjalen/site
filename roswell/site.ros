#! /bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#

(progn
  (ros:ensure-asdf)
  #+quicklisp (ql:quickload '(:site)))

(defun main (&rest argv)
  (declare (ignore argv))
  (site/main:safe-start-server))

